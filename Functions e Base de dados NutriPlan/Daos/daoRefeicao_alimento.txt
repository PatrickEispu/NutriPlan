public void adicionarAlimentoNaRefeicao(int idRefeicao, int idAlimento, int quantidade) {
    String sql = "SELECT adicionar_alimento_na_refeicao(?, ?, ?)";
    jdbcTemplate.update(sql, idRefeicao, idAlimento, quantidade);
}

public RefeicaoAlimento buscarAlimentoNaRefeicao(int idRefeicao, int idAlimento) {
    String sql = "SELECT * FROM buscar_alimento_na_refeicao(?, ?)";
    return jdbcTemplate.queryForObject(sql, new BeanPropertyRowMapper<>(RefeicaoAlimento.class), idRefeicao, idAlimento);
}

public void atualizarQuantidadeAlimentoNaRefeicao(int idRefeicao, int idAlimento, int quantidade) {
    String sql = "SELECT atualizar_quantidade_alimento_na_refeicao(?, ?, ?)";
    jdbcTemplate.update(sql, idRefeicao, idAlimento, quantidade);
}

public void removerAlimentoDaRefeicao(int idRefeicao, int idAlimento) {
    String sql = "SELECT remover_alimento_da_refeicao(?, ?)";
    jdbcTemplate.update(sql, idRefeicao, idAlimento);
}
